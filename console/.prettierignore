dist
node_modules
../internal/console/dist
playwright-report
test-results
e2e/__screenshots__

# Session state the auth setup writes, and the images the shots project
# writes. Both are local artifacts that never exist on a fresh checkout, so
# checking them only ever fails on the machine that ran the suite.
e2e/.auth
e2e/shots
package-lock.json
